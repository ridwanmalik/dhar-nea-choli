import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dhar-nea-choli',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
